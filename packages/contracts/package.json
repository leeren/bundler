{
  "name": "@account-abstraction/contracts",
  "description": "Account Abstraction (EIP 4337) contracts",
  "version": "0.5.0",
  "main": "dist/index.js",
  "scripts": {
    "clear": "rm -rf artifacts types dist",
    "build": "forge build",
    "compile": "yarn build && typechain --target ethers-v5 --out-dir types artifacts/**/*.json",
    "test": "forge test",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@gnosis.pm/safe-contracts": "^1.3.0"
  },
  "license": "MIT"
}
